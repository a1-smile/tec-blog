# Windows11 で MAMP を使用している環境で、
Composer をインストールしました

## あらかじめ やっておくこと
- MAMP の PHP にパスを通しておきます
以下の記事を参考にしてください。
[Windows11 における MAMP の PHP にパスを通す方法](https://qiita.com/a1-smile/items/b38783942ec8ef32f8a0)

なお、パス設定は、
- ざっくり、自分一人でPCを使う場合は、
ユーザー環境変数に設定でも、
システム環境変数に設定でも、どちらでも問題ありません。

- ユーザー環境変数に設定する場合は、
Composer を使用するユーザーごとに
パス設定をする必要があります

- 管理者権限やSYSTEM権限で composer を使用する場合は、
システム環境変数にパスを設定する必要があります

- システム環境変数に設定すれば、
すべてのユーザー、SYSTEM権限で使用可能です

- composer をすべてのユーザーで使用するという
設定にした場合でも、
パス設定をユーザー環境変数に設定しても
ユーザーとして使用する場合は、整合性に通常は問題ありません。
Windows は起動時に、有効なパスとして
ユーザー環境変数とシステム環境変数の両方を読み込むためです

- 一部の開発環境、実行環境では、
ユーザー環境変数のパスが有効にならない場合があります
これを回避するためには、システム環境変数にパスを設定します

- 環境変数の設定が PC 全体に影響することを避けたい場合は、
ユーザー環境変数に設定

## 注意すべき点
注意すべき点は、セットアップの
「Setting Check」画面で、
PHP のパスが正しく設定されていることを
確認するところです。

## インストールの手順
1. Composer の公式サイトにアクセスします。
   [https://getcomposer.org/](https://getcomposer.org/)
2. 「Download」ボタンをクリックします。
3. Windows Installer という欄の
    「Composer-Setup.exe」をクリックして、インストーラーをダウンロードします。
4. ダウンロードした「Composer-Setup.exe」をダブルクリックして、インストーラーを起動します。
5. セットアップの手順に従って、インストールを進めます。
6. コマンドプロンプトを開き、以下のコマンドを実行して、Composer が正しくインストールされているか確認します。
```
composer --version
```
## セットアップの説明
1. Select install mode 画面で
   「Install for all users」を選択。
   個人が一人で使う PC で管理者権限を持っている場合は、こちらを選択します。

2. Installation Options 画面で
   アンインストーラーなしでインストールという意味の
   記述に ✓ を入れずに「Next」をクリックします。

3. Setting Check 画面で
   PHP のパスが正しく設定されていることを確認します。
   正しく設定されていない場合は、PHP のパスを手動で指定する必要があります。
   例えば、MAMP の PHP のパスが
   `C:\MAMP\bin\php\php8.1.0` の場合は、以下のように指定します。
   
   「 C:\MAMP\bin\php\php8.1.0\php.exe 」

4. Proxy Settings 画面で
   インターネットにプロキシサーバーを介して接続している場合は、設定します。

   個人で自宅からインターネットに接続している場合は、そのまま「Next」をクリックします。

5. Ready to Install 画面で
   「Install」をクリックして、インストールを開始します。

6. Complete Composer Setup 画面で
   「Finish」をクリックして、インストールを完了します。

## インストール後の確認
1. コマンドプロンプトを開く、
開いている場合は再起動して、以下のコマンドを実行します。
```
composer --version
```
Composer のバージョンが表示されれば、インストールは成功です。




