# Windows11 における MAMP の PHP に Path を通す方法

## はじめに
Windows における MAMP の PHP に Path を通しました。
手順を共有します。

コマンドプロンプトや PowerShell で PHP のファイルを、
MAMP で使用している PHP で、
`php ファイル名.php` 
というコマンドを用いて実行できるようになります。

なお、環境変数に Path をコピーする際に、
ダブルクォーテーションで囲まないのが基本です。
理由は、
ダブルクォーテーションで囲むと、
ダブルクォーテーションこみで Path が認識されてしまい、
エラーになる可能性があります。
（ Claude にアドバイスされました。）
⭕正しい例
```
C:\MAMP\bin\php\php8.3.1
```
❌ダメな例
```
"C:\MAMP\bin\php\php8.3.1"
```
この記事が、
参考になれば幸いです。

## 開発環境
- OS: Windows 11 
- ローカル環境: MAMP (PHP バージョン: 8.3.1)

## 環境変数の種類について
Windows には、
ユーザー環境変数と
システム環境変数の
2種類があります。
- ユーザー環境変数: 現在ログインしているユーザーにのみ適用される環境変数
- システム環境変数: すべてのユーザーに適用される環境変数
- 両方にPHP の Path が設定されている場合は、ユーザー環境変数が優先されます

個人で開発している場合は、
最初はユーザー環境変数に Path を設定して、
必要に応じてシステム環境変数に Path を設定するのがおすすめです。

どちらの設定も手順はほぼ同じです。

## 手順の流れ
1. PHP のバージョンを確認する
2. MAMP の PHP の Path をコピー
3. 環境変数に Path を追加する
4. コマンドプロンプトで PHP のバージョンを確認する

次にバージョンの確認方法から順に説明します。

## PHP のバージョンを確認する
まず、MAMP の PHP のバージョンを確認します。
どのバージョンの PHP に Path を通すかを確認するためです。
私の環境では、PHP 8.3.1 でした。

MAMP
⬇️
Open WebStart page
⬇️
ツール
⬇️
phpInfo

で PHP のバージョンを確認できます。

## MAMP の PHP の Path をコピー
次に、MAMP の PHP の Path をコピーします。
win+E のショートカットキーでエクスプローラーを開きます。
C:
⬇️
MAMP
⬇️
bin
⬇️
php
⬇️
php8.3.1
とたどり、php8.3.1 を開き
php.exe があることを確認。
画面の上部にあるアドレスバーを右クリックして、
「アドレスをコピー」をクリックします。

これで、クリップボードに MAMP の PHP の Path がコピーされます。
例えば、私の場合は、以下のようになります。
```
C:\MAMP\bin\php\php8.3.1
```
## 環境変数に Path を追加する
次に、環境変数に Path を追加します。
1. win ボタンを押して、設定と入力して Enter キーを押します
2. 設定画面がひらいたら、システムをクリック。（左側にあります）
3. 「バージョン情報」をクリック。（私の環境では、一番下にあります）
4. 関連リンクという欄にある「システムの詳細設定」をクリック
5. 「システムのプロパティ」ウィンドウが開いたら、「詳細設定」タブをクリック

6. 「環境変数(N)」ボタンをクリック

7. 「ユーザー環境変数(U)」と「システム環境変数(S)」のうち、
「ユーザー環境変数(U)」の「Path」をクリックして、「編集(E)」をクリック
8. 「新規(N)」をクリックすると、入力欄が表示されるので、
先ほどコピーした MAMP の PHP の Path を貼り付け

例えば、私の場合は、以下のようになります。
```
C:\MAMP\bin\php\php8.3.1
```
「OK」をクリックして、すべてのウィンドウを閉じる。

## コマンドプロンプトで PHP のバージョンを確認する
最後に、コマンドプロンプトで PHP のバージョンを確認します。
1. win+R のショートカットキーで「ファイル名を指定して実行」を開きます
2. 「cmd」と入力して Enter キーを押すと、コマンドプロンプトが開きます
3. コマンドプロンプトで、以下のコマンドを入力して Enter キーを押します

```cmd
php -v
```
> ⚠️ 環境変数を変更した後は、
>コマンドプロンプトが開いているときは
> 一度閉じて、
> 新しく開き直してください。
>そうしないと、環境変数の変更が反映されません。

PHP のバージョンが表示されれば、MAMP の PHP に Path が通っていることになります。

